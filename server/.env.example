# Example environment variables for the local server
# Copy to server/.env and fill in values before running
NOTION_CLIENT_ID=
NOTION_CLIENT_SECRET=
PORT=3000
# (Optional) Path to sqlite DB (defaults to server/data/db.sqlite)
# DB_PATH=./data/db.sqlite
